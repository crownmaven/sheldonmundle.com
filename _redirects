# www → apex redirect (works on both Cloudflare Pages and Netlify).
# http → https is handled automatically by both providers, so no http source line is needed here.
https://www.sheldonmundle.com/* https://sheldonmundle.com/:splat 301
