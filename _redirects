# No path-based redirects defined.
#
# On Cloudflare Workers/Pages, hostname-prefixed sources are not allowed in
# this file. The www → apex redirect is handled at the domain-binding level
# instead (both domains attached to the Worker; apex set as the primary).
#
# On Netlify, http → https and www → apex are handled in netlify.toml plus
# Netlify's domain settings.
